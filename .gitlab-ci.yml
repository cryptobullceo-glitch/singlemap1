deploy:
  image: node:20-slim
  stage: deploy
  script:
    - echo "Account ID length=${#CLOUDFLARE_ACCOUNT_ID}"
    - npm install -g wrangler
    - wrangler pages deploy . --project-name=signalmap --branch=main
  only:
    - main
